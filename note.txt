dataset:
    training image (10), validation(5), testing image(3)

data handaling:
    training image : train 80% and validation 20%
    validation image: for testing 

image dim: 
    (smiple itk): 256 (slice), 128 (px size X), 256 (px size )
    (nibable): 256, 128, 256 (slice)

patch generator:
    size: 32 x 32 (image and label) (training)

image shape: 
    images: patch number, patche size (32 x 32), channel (1)
    label: patch number, patche size (32 x 32), channel (4)
